filebeat.inputs:
  - type: log
    paths:
      - /home/oncall/var/log/*/*.log

filebeat.autodiscover:
  providers:
    type: docker
    hints.enabled: true

processors:
  add_docker_metadata:

output.logstash:
  hosts: [ "logstash:5044" ]
